# 前端主题设置文档

本文档详细说明了 LLM ST FLY 项目前端主题系统的配置和使用方法。

## 📋 目录

- [主题概览](#主题概览)
- [主题架构](#主题架构)
- [使用主题](#使用主题)
- [自定义主题](#自定义主题)
- [组件适配](#组件适配)
- [最佳实践](#最佳实践)

## 主题概览

项目提供了三种精心设计的主题：

### 1. 浅色主题（Light Theme）
- **特点**: 清新明亮，适合白天使用
- **主色调**: 蓝色 (#3b82f6)
- **背景色**: 白色系 (#ffffff, #f8fafc)
- **文字色**: 深灰色 (#1f2937)

### 2. 深色主题（Dark Theme）
- **特点**: 护眼舒适，适合夜间使用
- **主色调**: 亮蓝色 (#5e9bff)
- **背景色**: 深蓝灰 (#0f172a, #1e293b)
- **文字色**: 浅灰白 (#e1e6f5)

### 3. 未来科技主题（Future Theme）
- **特点**: 赛博朋克风格，炫酷科技感
- **主色调**: 霓虹青蓝 (#00d4ff)
- **辅助色**: 霓虹粉 (#ff0080)、深紫 (#7c3aed)
- **背景色**: 深空渐变
- **特效**: 渐变边框、霓虹光晕、脉冲动画

## 主题架构

### 文件结构

```
frontend/src/
├── styles/
│   └── themes.css          # 主题 CSS 变量定义
├── stores/
│   └── theme.ts            # 主题状态管理
├── types/
│   └── theme.ts            # 主题类型定义
└── utils/
    └── themeUtils.ts       # 主题工具函数
```

### 主题变量系统

主题系统基于 CSS 自定义属性（CSS Variables）实现，所有颜色和样式都通过变量定义。

#### 基础颜色变量

```css
:root {
  /* 主题色 */
  --color-primary: #5e9bff;
  --color-secondary: #9ca3af;
  --color-accent: #a569ff;
  
  /* 状态色 */
  --color-success: #22c55e;
  --color-warning: #fbbf24;
  --color-error: #f87171;
  --color-info: #38bdf8;
  
  /* 背景色 */
  --color-background: #0f172a;
  --color-surface: #1e293b;
  
  /* 文字色 */
  --color-text: #e1e6f5;
  --color-textSecondary: #94a3b8;
  
  /* 边框色 */
  --color-border: #334155;
}
```

#### 组件级变量

```css
:root {
  /* 侧边栏 */
  --sidebar-background: linear-gradient(180deg, #1a2233 0%, #0c1425 100%);
  --sidebar-text: #e1e6f5;
  --sidebar-activeBackground: rgba(94, 155, 255, 0.15);
  --sidebar-activeText: #5e9bff;
  
  /* 头部 */
  --header-background: #1e293b;
  --header-text: #e1e6f5;
  --header-border: #334155;
  
  /* 卡片 */
  --card-background: #1e293b;
  --card-border: #334155;
  --card-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.3);
}
```

#### 按钮系统变量

```css
:root {
  /* 主要按钮 */
  --button-primary: #5e9bff;
  --button-primaryHover: #4f8aff;
  --button-primaryActive: #3d7eff;
  --button-primaryText: #ffffff;
  
  /* 次要按钮 */
  --button-secondary: #64748b;
  --button-secondaryHover: #475569;
  --button-secondaryText: #e1e6f5;
  
  /* 成功按钮 */
  --button-success: #22c55e;
  --button-successHover: #16a34a;
  --button-successText: #ffffff;
  
  /* 其他按钮类型... */
}
```

## 使用主题

### 1. 在 Store 中管理主题

```typescript
// stores/theme.ts
import { defineStore } from 'pinia'
import { ref } from 'vue'

export const useThemeStore = defineStore('theme', () => {
  const currentTheme = ref<ThemeType>('dark')
  
  // 设置主题
  const setTheme = (theme: ThemeType) => {
    currentTheme.value = theme
    localStorage.setItem('app-theme', theme)
    applyTheme()
  }
  
  // 应用主题到 DOM
  const applyTheme = () => {
    document.body.className = `theme-${currentTheme.value}`
    // 应用 CSS 变量...
  }
  
  return {
    currentTheme,
    setTheme
  }
})
```

### 2. 在组件中使用主题

```vue
<script setup lang="ts">
import { useThemeStore } from '@/stores/theme'

const themeStore = useThemeStore()

// 切换主题
const changeTheme = (theme: 'light' | 'dark' | 'future') => {
  themeStore.setTheme(theme)
}
</script>

<template>
  <div class="theme-selector">
    <button @click="changeTheme('light')">浅色</button>
    <button @click="changeTheme('dark')">深色</button>
    <button @click="changeTheme('future')">未来</button>
  </div>
</template>
```

### 3. 在样式中使用主题变量

```vue
<style scoped>
.card {
  background: var(--card-background);
  border: 1px solid var(--card-border);
  box-shadow: var(--card-shadow);
  color: var(--color-text);
}

.primary-button {
  background: var(--button-primary);
  color: var(--button-primaryText);
}

.primary-button:hover {
  background: var(--button-primaryHover);
}
</style>
```

## 自定义主题

### 1. 创建新主题

在 `frontend/src/styles/themes.css` 中添加新主题：

```css
/* 自定义主题 */
.theme-custom {
  /* 基础颜色 */
  --color-primary: #your-primary-color;
  --color-secondary: #your-secondary-color;
  --color-background: #your-background-color;
  --color-surface: #your-surface-color;
  --color-text: #your-text-color;
  --color-textSecondary: #your-text-secondary-color;
  --color-border: #your-border-color;
  
  /* 状态色 */
  --color-success: #your-success-color;
  --color-warning: #your-warning-color;
  --color-error: #your-error-color;
  --color-info: #your-info-color;
  
  /* 组件样式 */
  --sidebar-background: your-sidebar-style;
  --card-background: your-card-style;
  /* 其他变量... */
}
```

### 2. 注册新主题

在 `frontend/src/stores/theme.ts` 中注册：

```typescript
const themes: Record<ThemeType, ThemeConfig> = {
  // ... 现有主题
  custom: {
    name: 'custom',
    label: '自定义主题',
    colors: {
      primary: '#your-primary-color',
      // ... 其他颜色
    },
    // ... 其他配置
  }
}
```

### 3. 添加主题选择器

```vue
<template>
  <div class="theme-selector">
    <button
      v-for="theme in themeList"
      :key="theme.name"
      @click="setTheme(theme.name)"
      :class="{ active: currentTheme === theme.name }"
    >
      {{ theme.label }}
    </button>
  </div>
</template>
```

## 组件适配

### 1. 使用主题变量

**好的例子**：
```css
.user-card {
  background: var(--color-surface);
  color: var(--color-text);
  border: 1px solid var(--color-border);
}
```

**不好的例子**（硬编码颜色）：
```css
.user-card {
  background: #1e293b;  /* ❌ 不要这样做 */
  color: #e1e6f5;       /* ❌ 不要这样做 */
}
```

### 2. 按钮适配

```vue
<template>
  <button class="btn btn-primary">主要按钮</button>
  <button class="btn btn-secondary">次要按钮</button>
  <button class="btn btn-success">成功按钮</button>
</template>

<style scoped>
.btn {
  padding: 10px 20px;
  border: none;
  border-radius: 8px;
  cursor: pointer;
  transition: all 0.3s;
}

.btn-primary {
  background: var(--button-primary);
  color: var(--button-primaryText);
}

.btn-primary:hover {
  background: var(--button-primaryHover);
}

.btn-secondary {
  background: var(--button-secondary);
  color: var(--button-secondaryText);
}

.btn-success {
  background: var(--button-success);
  color: var(--button-successText);
}
</style>
```

### 3. 表单适配

```vue
<style scoped>
.form-input {
  background: var(--color-background);
  color: var(--color-text);
  border: 1px solid var(--color-border);
  padding: 10px;
  border-radius: 6px;
}

.form-input:focus {
  border-color: var(--color-primary);
  box-shadow: 0 0 0 3px rgba(var(--color-primary-rgb), 0.2);
}

.form-label {
  color: var(--color-text);
  margin-bottom: 8px;
}
</style>
```

### 4. 卡片适配

```vue
<style scoped>
.card {
  background: var(--card-background);
  border: 1px solid var(--card-border);
  box-shadow: var(--card-shadow);
  border-radius: 12px;
  padding: 20px;
  color: var(--color-text);
}

.card-title {
  color: var(--color-text);
  font-size: 18px;
  font-weight: 600;
  margin-bottom: 12px;
}

.card-description {
  color: var(--color-textSecondary);
  font-size: 14px;
}
</style>
```

## 主题特效

### 未来科技主题特效

#### 1. 霓虹光晕

```css
.theme-future .neon-glow {
  box-shadow: 
    0 0 10px rgba(0, 212, 255, 0.3),
    0 0 20px rgba(255, 0, 110, 0.2),
    0 0 30px rgba(124, 58, 237, 0.1);
}
```

#### 2. 渐变边框

```css
.theme-future .gradient-border {
  position: relative;
  background: var(--card-background);
}

.theme-future .gradient-border::before {
  content: '';
  position: absolute;
  inset: 0;
  border-radius: inherit;
  padding: 1px;
  background: linear-gradient(90deg, #00d4ff 0%, #ff0080 100%);
  -webkit-mask: linear-gradient(#fff 0 0) content-box, 
                linear-gradient(#fff 0 0);
  -webkit-mask-composite: xor;
  mask-composite: exclude;
}
```

#### 3. 脉冲动画

```css
@keyframes pulse-glow {
  0%, 100% {
    box-shadow: 0 0 10px rgba(0, 212, 255, 0.3);
  }
  50% {
    box-shadow: 0 0 20px rgba(0, 212, 255, 0.6);
  }
}

.theme-future .pulse {
  animation: pulse-glow 2s ease-in-out infinite;
}
```

#### 4. 滚动条特效

```css
.theme-future ::-webkit-scrollbar-thumb {
  background: linear-gradient(180deg, 
    rgba(0, 212, 255, 0.6) 0%, 
    rgba(124, 58, 237, 0.6) 50%,
    rgba(255, 0, 110, 0.6) 100%
  );
  animation: future-glow 3s ease-in-out infinite alternate;
}

@keyframes future-glow {
  0% {
    box-shadow: 0 0 10px rgba(0, 212, 255, 0.2);
  }
  100% {
    box-shadow: 0 0 15px rgba(0, 212, 255, 0.3);
  }
}
```

## 响应式主题

### 1. 移动端适配

```css
@media (max-width: 768px) {
  :root {
    /* 调整字体大小 */
    --font-size-base: 14px;
    
    /* 调整间距 */
    --spacing-base: 16px;
    
    /* 调整按钮大小 */
    --button-height: 40px;
  }
}
```

### 2. 暗色模式检测

```css
@media (prefers-color-scheme: dark) {
  /* 如果用户系统设置为暗色模式，可以自动应用 */
  :root {
    --color-background: #0f172a;
    --color-text: #e1e6f5;
  }
}
```

## 主题过渡动画

### 1. 平滑过渡

```typescript
// utils/themeUtils.ts
export class ThemeAnimations {
  static createTransition(duration: number = 300) {
    const style = document.createElement('style')
    style.innerHTML = `
      * {
        transition: background-color ${duration}ms ease,
                    color ${duration}ms ease,
                    border-color ${duration}ms ease !important;
      }
    `
    document.head.appendChild(style)
    
    setTimeout(() => {
      document.head.removeChild(style)
    }, duration)
  }
}
```

### 2. 在切换时使用

```typescript
const setTheme = (theme: ThemeType) => {
  // 添加过渡动画
  ThemeAnimations.createTransition(300)
  
  currentTheme.value = theme
  localStorage.setItem('app-theme', theme)
  applyTheme()
}
```

## 最佳实践

### 1. 始终使用 CSS 变量

✅ **好的做法**：
```css
.button {
  background: var(--button-primary);
  color: var(--button-primaryText);
}
```

❌ **不好的做法**：
```css
.button {
  background: #5e9bff;
  color: #ffffff;
}
```

### 2. 使用语义化的变量名

✅ **好的做法**：
```css
.success-message {
  background: var(--color-success);
}
```

❌ **不好的做法**：
```css
.success-message {
  background: var(--color-green);
}
```

### 3. 提供降级方案

```css
.card {
  /* 提供默认值 */
  background: var(--card-background, #1e293b);
  color: var(--color-text, #e1e6f5);
}
```

### 4. 组织 CSS 变量

```css
:root {
  /* 1. 基础颜色 */
  --color-primary: #5e9bff;
  --color-secondary: #9ca3af;
  
  /* 2. 状态颜色 */
  --color-success: #22c55e;
  --color-error: #f87171;
  
  /* 3. 背景颜色 */
  --color-background: #0f172a;
  --color-surface: #1e293b;
  
  /* 4. 文字颜色 */
  --color-text: #e1e6f5;
  --color-textSecondary: #94a3b8;
  
  /* 5. 组件样式 */
  --button-primary: var(--color-primary);
  --card-background: var(--color-surface);
}
```

### 5. 避免过度嵌套

✅ **好的做法**：
```css
.card-title {
  color: var(--color-text);
}

.card-description {
  color: var(--color-textSecondary);
}
```

❌ **不好的做法**：
```css
.card .content .title {
  color: var(--color-text);
}

.card .content .description {
  color: var(--color-textSecondary);
}
```

## 主题调试

### 1. 查看当前主题变量

在浏览器控制台执行：

```javascript
// 获取所有 CSS 变量
const styles = getComputedStyle(document.documentElement)
const primary = styles.getPropertyValue('--color-primary')
console.log('Primary Color:', primary)
```

### 2. 动态修改主题变量

```javascript
// 临时修改主题变量（用于调试）
document.documentElement.style.setProperty('--color-primary', '#ff0000')
```

### 3. 主题验证工具

```typescript
// 验证所有必需的主题变量是否存在
const validateTheme = () => {
  const requiredVars = [
    '--color-primary',
    '--color-background',
    '--color-text',
    '--button-primary'
  ]
  
  const styles = getComputedStyle(document.documentElement)
  
  requiredVars.forEach(varName => {
    const value = styles.getPropertyValue(varName)
    if (!value) {
      console.warn(`Missing theme variable: ${varName}`)
    }
  })
}
```

## 常见问题

### Q1: 主题切换后某些组件没有变化？

**原因**: 可能使用了硬编码的颜色值。

**解决**: 检查组件样式，确保使用 CSS 变量：
```css
/* 修改前 */
.component {
  background: #1e293b;
}

/* 修改后 */
.component {
  background: var(--color-surface);
}
```

### Q2: 如何为第三方组件应用主题？

**方案 1**: 使用全局样式覆盖
```css
/* styles/themes.css */
.el-button--primary {
  background: var(--button-primary) !important;
}
```

**方案 2**: 在组件中动态设置
```vue
<script setup>
import { useThemeStore } from '@/stores/theme'

const themeStore = useThemeStore()
const buttonStyle = computed(() => ({
  background: themeStore.theme.colors.primary
}))
</script>

<template>
  <el-button :style="buttonStyle">按钮</el-button>
</template>
```

### Q3: 主题变量在 JavaScript 中如何获取？

```typescript
// 方法 1: 从 store 获取
import { useThemeStore } from '@/stores/theme'
const themeStore = useThemeStore()
const primaryColor = themeStore.theme.colors.primary

// 方法 2: 从 DOM 获取
const styles = getComputedStyle(document.documentElement)
const primaryColor = styles.getPropertyValue('--color-primary').trim()
```

### Q4: 如何实现主题预览？

```vue
<template>
  <div class="theme-preview">
    <div 
      v-for="theme in themes"
      :key="theme.name"
      :class="`theme-${theme.name}`"
      class="preview-card"
    >
      <h3>{{ theme.label }}</h3>
      <div class="color-samples">
        <span class="primary-sample"></span>
        <span class="success-sample"></span>
        <span class="warning-sample"></span>
      </div>
    </div>
  </div>
</template>

<style scoped>
.preview-card {
  background: var(--card-background);
  border: 1px solid var(--card-border);
  padding: 20px;
}

.primary-sample {
  background: var(--color-primary);
  /* ... */
}
</style>
```

## 主题扩展

### 添加自定义属性

```css
.theme-custom {
  /* 自定义间距系统 */
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;
  
  /* 自定义圆角 */
  --radius-sm: 4px;
  --radius-md: 8px;
  --radius-lg: 12px;
  --radius-full: 9999px;
  
  /* 自定义字体大小 */
  --font-xs: 12px;
  --font-sm: 14px;
  --font-md: 16px;
  --font-lg: 18px;
  --font-xl: 24px;
}
```

### 使用自定义属性

```vue
<style scoped>
.container {
  padding: var(--spacing-md);
  border-radius: var(--radius-lg);
  font-size: var(--font-md);
}
</style>
```

## 总结

遵循本文档的规范和最佳实践，可以：

1. ✅ 创建统一美观的用户界面
2. ✅ 轻松实现多主题切换
3. ✅ 简化组件样式维护
4. ✅ 提供更好的用户体验
5. ✅ 快速自定义和扩展主题

**记住：好的主题系统不仅美观，还要易用、易维护、易扩展！**


# API 接口说明文档

本文档详细说明了 LLM ST FLY 项目的所有 API 接口。

## 📋 目录

- [统一响应格式](#统一响应格式)
- [认证相关接口](#认证相关接口)
- [用户管理接口](#用户管理接口)
- [AI 对话接口](#ai-对话接口)
- [AI 图像生成接口](#ai-图像生成接口)
- [AI 音频处理接口](#ai-音频处理接口)
- [AI 视频生成接口](#ai-视频生成接口)
- [智能体接口](#智能体接口)
- [日志管理接口](#日志管理接口)

## 统一响应格式

所有 API 接口都遵循统一的响应格式：

### 成功响应

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {},
  "timestamp": "2025-01-08T19:00:00Z",
  "request_id": "uuid-string"
}
```

### 错误响应

```json
{
  "code": 400,
  "message": "参数验证失败",
  "data": {
    "errors": {
      "username": ["用户名不能为空"],
      "email": ["邮箱格式不正确"]
    }
  },
  "timestamp": "2025-01-08T19:00:00Z",
  "request_id": "uuid-string"
}
```

### 状态码说明

| 状态码 | 说明 |
|--------|------|
| 200 | 成功 |
| 201 | 创建成功 |
| 400 | 客户端错误（参数错误等） |
| 401 | 未授权 |
| 403 | 禁止访问 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

## 认证相关接口

### 用户登录

**接口地址**: `POST /api/v1/auth/login/`

**请求参数**:
```json
{
  "username": "admin",
  "password": "password123"
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "user": {
      "id": 1,
      "username": "admin",
      "email": "admin@example.com",
      "role": "admin",
      "avatar": "http://localhost:8000/media/avatars/default.png",
      "nickname": "管理员"
    },
    "token": {
      "refresh": "eyJ0eXAiOiJKV1QiLCJhbGc...",
      "access": "eyJ0eXAiOiJKV1QiLCJhbGc..."
    }
  }
}
```

### 用户注册

**接口地址**: `POST /api/v1/auth/register/`

**请求参数**:
```json
{
  "username": "newuser",
  "email": "newuser@example.com",
  "password": "password123",
  "password_confirm": "password123"
}
```

**响应示例**:
```json
{
  "code": 201,
  "message": "注册成功",
  "data": {
    "id": 2,
    "username": "newuser",
    "email": "newuser@example.com"
  }
}
```

### 刷新令牌

**接口地址**: `POST /api/v1/auth/token/refresh/`

**请求参数**:
```json
{
  "refresh": "eyJ0eXAiOiJKV1QiLCJhbGc..."
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "令牌刷新成功",
  "data": {
    "access": "eyJ0eXAiOiJKV1QiLCJhbGc..."
  }
}
```

### 修改密码

**接口地址**: `POST /api/v1/auth/users/change_password/`

**请求头**: 需要 Authorization Bearer Token

**请求参数**:
```json
{
  "old_password": "oldpass123",
  "new_password": "newpass123",
  "new_password_confirm": "newpass123"
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "密码修改成功",
  "data": null
}
```

## 用户管理接口

### 获取用户列表

**接口地址**: `GET /api/v1/auth/user-management/`

**请求头**: 需要 Authorization Bearer Token

**查询参数**:
- `page`: 页码（可选，默认 1）
- `page_size`: 每页数量（可选，默认 20）
- `search`: 搜索关键词（可选）

**响应示例**:
```json
{
  "code": 200,
  "message": "获取用户列表成功",
  "data": {
    "list": [
      {
        "id": 1,
        "username": "admin",
        "email": "admin@example.com",
        "role": {
          "id": 1,
          "name": "管理员"
        },
        "is_active": true,
        "date_joined": "2025-01-01T00:00:00Z"
      }
    ],
    "total": 100,
    "page": 1,
    "page_size": 20,
    "total_pages": 5,
    "has_next": true,
    "has_previous": false
  }
}
```

### 获取当前用户信息

**接口地址**: `GET /api/v1/auth/users/me/`

**请求头**: 需要 Authorization Bearer Token

**响应示例**:
```json
{
  "code": 200,
  "message": "获取用户信息成功",
  "data": {
    "id": 1,
    "username": "admin",
    "email": "admin@example.com",
    "avatar": "http://localhost:8000/media/avatars/avatar.png",
    "nickname": "管理员",
    "role": "admin",
    "real_name": "张三",
    "phone": "13800138000",
    "department": "技术部"
  }
}
```

### 更新用户信息

**接口地址**: `PUT /api/v1/auth/users/me/`

**请求头**: 
- Authorization: Bearer Token
- Content-Type: multipart/form-data

**请求参数** (FormData):
```
username: admin
nickname: 新昵称
email: newemail@example.com
avatar: [文件]
phone: 13800138000
```

**响应示例**:
```json
{
  "code": 200,
  "message": "更新成功",
  "data": {
    "id": 1,
    "username": "admin",
    "email": "newemail@example.com",
    "avatar": "http://localhost:8000/media/avatars/new_avatar.png"
  }
}
```

### 创建用户

**接口地址**: `POST /api/v1/auth/user-management/`

**请求头**: 需要 Authorization Bearer Token（管理员权限）

**请求参数**:
```json
{
  "username": "newuser",
  "email": "newuser@example.com",
  "password": "password123",
  "user_role": 2,
  "is_active": true
}
```

**响应示例**:
```json
{
  "code": 201,
  "message": "用户创建成功",
  "data": {
    "id": 5,
    "username": "newuser",
    "email": "newuser@example.com"
  }
}
```

### 删除用户

**接口地址**: `DELETE /api/v1/auth/user-management/{id}/`

**请求头**: 需要 Authorization Bearer Token（管理员权限）

**响应示例**:
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

## AI 对话接口

### 流式对话（推荐）

**接口地址**: `POST /api/v1/chat/ai-chat/stream-chat/`

**请求头**: 
- Authorization: Bearer Token
- Content-Type: application/json
- Accept: text/event-stream

**请求参数**:
```json
{
  "messages": [
    {
      "role": "user",
      "content": "你好"
    }
  ],
  "conversation_id": 1,
  "deep_thinking": false,
  "web_search": false,
  "stream": true
}
```

**响应格式**: Server-Sent Events (SSE)

```
data: {"type": "conversation_id", "conversation_id": 1}

data: {"type": "thinking", "content": "思考中...", "full_thinking": "..."}

data: {"type": "content", "content": "你好", "full_content": "你好！"}

data: {"type": "final", "content": "你好！我是AI助手", "usage": {...}}

data: {"type": "done"}
```

### 获取对话列表

**接口地址**: `GET /api/v1/chat/conversations/`

**请求头**: 需要 Authorization Bearer Token

**查询参数**:
- `type`: 对话类型（可选：`default`, `ai-chat`）

**响应示例**:
```json
{
  "code": 200,
  "message": "获取对话列表成功",
  "data": {
    "list": [
      {
        "id": 1,
        "title": "对话主题",
        "custom_title": "我的对话",
        "display_title": "我的对话",
        "created_at": "2025-01-01T00:00:00Z",
        "updated_at": "2025-01-02T00:00:00Z",
        "message_count": 10,
        "is_pinned": false
      }
    ],
    "total": 50
  }
}
```

### 创建对话

**接口地址**: `POST /api/v1/chat/conversations/`

**请求头**: 需要 Authorization Bearer Token

**请求参数**:
```json
{
  "title": "新对话"
}
```

**响应示例**:
```json
{
  "code": 201,
  "message": "创建成功",
  "data": {
    "id": 2,
    "title": "新对话",
    "created_at": "2025-01-08T19:00:00Z"
  }
}
```

### 删除对话

**接口地址**: `DELETE /api/v1/chat/conversations/{id}/`

**请求头**: 需要 Authorization Bearer Token

**响应示例**:
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

### 置顶对话

**接口地址**: `POST /api/v1/chat/conversations/{id}/pin/`

**请求头**: 需要 Authorization Bearer Token

**响应示例**:
```json
{
  "code": 200,
  "message": "置顶成功",
  "data": {
    "is_pinned": true
  }
}
```

### 重命名对话

**接口地址**: `PATCH /api/v1/chat/conversations/{id}/rename/`

**请求头**: 需要 Authorization Bearer Token

**请求参数**:
```json
{
  "custom_title": "新标题"
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "重命名成功",
  "data": {
    "custom_title": "新标题"
  }
}
```

## AI 图像生成接口

### 生成图像

**接口地址**: `POST /api/v1/ai-image/generate/`

**请求头**: 需要 Authorization Bearer Token

**请求参数**:
```json
{
  "prompt": "一只可爱的小猫",
  "negative_prompt": "模糊，低质量",
  "size": "1024*1024",
  "n": 1,
  "prompt_extend": true,
  "watermark": false,
  "style": "auto",
  "shot_type": "close-up",
  "angle": "eye-level",
  "lighting": "natural"
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "生成成功",
  "data": {
    "task_id": "task-123",
    "status": "SUCCESS",
    "images": [
      {
        "original_url": "https://...",
        "saved_url": "http://localhost:8000/media/ai-images/image.png",
        "filename": "image.png",
        "size": 1024000
      }
    ],
    "enhanced_prompt": "一只可爱的小猫，高清，细节丰富",
    "usage": {
      "total_tokens": 100
    }
  }
}
```

### 获取预设参数

**接口地址**: `GET /api/v1/ai-image/presets/`

**请求头**: 需要 Authorization Bearer Token

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "sizes": [
      {"value": "1024*1024", "label": "正方形", "aspect_ratio": "1:1"},
      {"value": "720*1280", "label": "竖屏", "aspect_ratio": "9:16"}
    ],
    "styles": [
      {"value": "auto", "label": "自动"},
      {"value": "photographic", "label": "摄影"}
    ],
    "shot_types": [
      {"value": "close-up", "label": "特写"},
      {"value": "wide-shot", "label": "全景"}
    ]
  }
}
```

### 获取生成历史

**接口地址**: `GET /api/v1/ai-image/history/`

**请求头**: 需要 Authorization Bearer Token

**查询参数**:
- `page`: 页码（默认 1）
- `page_size`: 每页数量（默认 20）

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "list": [
      {
        "task_id": "task-123",
        "prompt": "一只可爱的小猫",
        "images": [...],
        "created_at": "2025-01-08T19:00:00Z"
      }
    ],
    "total": 50,
    "page": 1,
    "page_size": 20
  }
}
```

## AI 音频处理接口

### 语音转文字

**接口地址**: `POST /api/v1/chat/ai-audio/speech-to-text/`

**请求头**: 
- Authorization: Bearer Token
- Content-Type: multipart/form-data

**请求参数** (FormData):
```
audio_file: [音频文件]
language: zh
model: paraformer-v2
```

**响应示例**:
```json
{
  "code": 200,
  "message": "转换成功",
  "data": {
    "task_id": "audio-task-123",
    "status": "COMPLETED",
    "task_type": "speech_to_text",
    "result_text": "这是转换后的文字内容",
    "confidence": 0.95,
    "duration": 10.5,
    "created_at": "2025-01-08T19:00:00Z"
  }
}
```

### 文字转语音

**接口地址**: `POST /api/v1/chat/ai-audio/text-to-speech/`

**请求头**: 需要 Authorization Bearer Token

**请求参数**:
```json
{
  "text": "你好，我是AI助手",
  "voice": "aixia",
  "language_type": "zh",
  "format": "mp3"
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "转换成功",
  "data": {
    "task_id": "audio-task-456",
    "status": "COMPLETED",
    "task_type": "text_to_speech",
    "result_url": "http://localhost:8000/media/ai-audio/output.mp3",
    "duration": 5.2,
    "file_size": 125000,
    "created_at": "2025-01-08T19:00:00Z"
  }
}
```

### 语音克隆

**接口地址**: `POST /api/v1/chat/ai-audio/voice-clone/`

**请求头**: 
- Authorization: Bearer Token
- Content-Type: multipart/form-data

**请求参数** (FormData):
```
reference_audio: [参考音频文件]
reference_text: 参考音频的文字内容
target_text: 要克隆生成的文字内容
model: cosyvoice-v1
```

**响应示例**:
```json
{
  "code": 200,
  "message": "克隆成功",
  "data": {
    "task_id": "audio-task-789",
    "status": "COMPLETED",
    "task_type": "voice_clone",
    "result_url": "http://localhost:8000/media/ai-audio/cloned.mp3",
    "duration": 8.5,
    "created_at": "2025-01-08T19:00:00Z"
  }
}
```

## AI 视频生成接口

### 文生视频

**接口地址**: `POST /api/v1/chat/ai-video/text-to-video/`

**请求头**: 需要 Authorization Bearer Token

**请求参数**:
```json
{
  "prompt": "一只小猫在草地上玩耍",
  "model": "cogvideox-5b",
  "resolution": "720p",
  "duration": 6,
  "fps": 16,
  "seed": 12345
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "任务已创建",
  "data": {
    "task_id": "video-task-123",
    "status": "PENDING",
    "task_type": "text_to_video",
    "created_at": "2025-01-08T19:00:00Z"
  }
}
```

### 图生视频

**接口地址**: `POST /api/v1/chat/ai-video/image-to-video/`

**请求头**: 
- Authorization: Bearer Token
- Content-Type: multipart/form-data

**请求参数** (FormData):
```
image_file: [图片文件]
prompt: 让图片动起来
model: cogvideox-5b
resolution: 720p
duration: 6
fps: 16
```

**响应示例**:
```json
{
  "code": 200,
  "message": "任务已创建",
  "data": {
    "task_id": "video-task-456",
    "status": "PENDING",
    "task_type": "image_to_video",
    "created_at": "2025-01-08T19:00:00Z"
  }
}
```

### 查询视频任务状态

**接口地址**: `GET /api/v1/chat/ai-video/task/{task_id}/status/`

**请求头**: 需要 Authorization Bearer Token

**响应示例**:
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "task_id": "video-task-123",
    "status": "COMPLETED",
    "task_type": "text_to_video",
    "result_url": "http://localhost:8000/media/ai-videos/video.mp4",
    "thumbnail_url": "http://localhost:8000/media/ai-videos/thumb.jpg",
    "duration": 6,
    "resolution": "720p",
    "fps": 16,
    "file_size": 5242880,
    "created_at": "2025-01-08T19:00:00Z",
    "completed_at": "2025-01-08T19:05:00Z"
  }
}
```

### 获取视频历史

**接口地址**: `GET /api/v1/chat/ai-video/history/`

**请求头**: 需要 Authorization Bearer Token

**查询参数**:
- `task_type`: 任务类型（可选：`text_to_video`, `image_to_video`）
- `limit`: 返回数量（默认 20）

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "task_id": "video-task-123",
      "task_type": "text_to_video",
      "status": "COMPLETED",
      "result_url": "http://localhost:8000/media/ai-videos/video.mp4",
      "created_at": "2025-01-08T19:00:00Z"
    }
  ]
}
```

## 智能体接口

### 客服助手 - 获取助手列表

**接口地址**: `GET /api/v1/agent/customer-service/assistants/`

**请求头**: 需要 Authorization Bearer Token

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "id": 1,
      "name": "技术支持助手",
      "avatar": "🤖",
      "description": "专业的技术支持客服",
      "greeting_message": "您好！我是技术支持助手",
      "is_default": true,
      "knowledge_bases_info": [
        {
          "id": 1,
          "name": "技术文档",
          "status": "ready"
        }
      ],
      "created_at": "2025-01-01T00:00:00Z"
    }
  ]
}
```

### 客服助手 - 创建助手

**接口地址**: `POST /api/v1/agent/customer-service/assistants/`

**请求头**: 需要 Authorization Bearer Token

**请求参数**:
```json
{
  "name": "销售助手",
  "avatar": "👔",
  "description": "专业的销售咨询客服",
  "greeting_message": "您好！有什么可以帮您？",
  "system_prompt": "你是一位专业的销售顾问...",
  "knowledge_bases": [1, 2],
  "is_default": false
}
```

**响应示例**:
```json
{
  "code": 201,
  "message": "创建成功",
  "data": {
    "id": 2,
    "name": "销售助手",
    "avatar": "👔"
  }
}
```

### 客服助手 - 获取知识库列表

**接口地址**: `GET /api/v1/agent/customer-service/knowledge-bases/`

**请求头**: 需要 Authorization Bearer Token

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "id": 1,
      "name": "产品手册",
      "description": "产品功能和使用说明",
      "format": "pdf",
      "status": "ready",
      "document_count": 5,
      "total_chunks": 150,
      "created_at": "2025-01-01T00:00:00Z"
    }
  ]
}
```

### 客服助手 - 创建知识库

**接口地址**: `POST /api/v1/agent/customer-service/knowledge-bases/`

**请求头**: 
- Authorization: Bearer Token
- Content-Type: multipart/form-data

**请求参数** (FormData):
```
name: 产品手册
description: 产品功能和使用说明
format: pdf
```

**响应示例**:
```json
{
  "code": 201,
  "message": "创建成功",
  "data": {
    "id": 2,
    "name": "产品手册",
    "status": "processing"
  }
}
```

### 客服助手 - 上传文档

**接口地址**: `POST /api/v1/agent/customer-service/documents/`

**请求头**: 
- Authorization: Bearer Token
- Content-Type: multipart/form-data

**请求参数** (FormData):
```
knowledge_base: 1
file: [文档文件]
title: 产品说明书
```

**响应示例**:
```json
{
  "code": 201,
  "message": "上传成功",
  "data": {
    "id": 5,
    "title": "产品说明书",
    "file_type": "pdf",
    "status": "processing"
  }
}
```

### 客服助手 - 对话

**接口地址**: `POST /api/v1/agent/customer-service/chat/`

**请求头**: 需要 Authorization Bearer Token

**请求参数**:
```json
{
  "message": "如何使用这个功能？",
  "session_id": "session-123",
  "assistant_id": 1
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "回复成功",
  "data": {
    "session_id": "session-123",
    "response": "使用这个功能很简单...",
    "intent": "retrieval",
    "confidence": 0.92,
    "sources": [
      {
        "title": "用户手册",
        "content": "相关内容片段..."
      }
    ]
  }
}
```

### 客服助手 - 获取推荐问题

**接口地址**: `POST /api/v1/agent/customer-service/recommended-questions/`

**请求头**: 需要 Authorization Bearer Token

**请求参数**:
```json
{
  "knowledge_base_ids": [1, 2],
  "limit": 4
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    "如何注册账号？",
    "密码忘记了怎么办？",
    "如何修改个人信息？",
    "支持哪些支付方式？"
  ]
}
```

## 日志管理接口

### 获取系统日志

**接口地址**: `GET /api/v1/logs/system/`

**请求头**: 需要 Authorization Bearer Token（管理员权限）

**查询参数**:
- `page`: 页码（默认 1）
- `page_size`: 每页数量（默认 20）
- `level`: 日志级别（可选：DEBUG, INFO, WARNING, ERROR）

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "list": [
      {
        "id": 1,
        "level": "INFO",
        "message": "系统启动",
        "timestamp": "2025-01-08T19:00:00Z"
      }
    ],
    "total": 1000,
    "page": 1,
    "page_size": 20
  }
}
```

### 获取用户日志

**接口地址**: `GET /api/v1/logs/user/`

**请求头**: 需要 Authorization Bearer Token（管理员权限）

**查询参数**:
- `page`: 页码
- `page_size`: 每页数量
- `user_id`: 用户ID（可选）
- `action`: 操作类型（可选）

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "list": [
      {
        "id": 1,
        "user": "admin",
        "action": "login",
        "ip": "127.0.0.1",
        "timestamp": "2025-01-08T19:00:00Z"
      }
    ],
    "total": 500
  }
}
```

### 获取 API 日志

**接口地址**: `GET /api/v1/logs/api/`

**请求头**: 需要 Authorization Bearer Token（管理员权限）

**查询参数**:
- `page`: 页码
- `page_size`: 每页数量
- `method`: HTTP 方法（可选）
- `status_code`: 状态码（可选）

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "list": [
      {
        "id": 1,
        "method": "POST",
        "path": "/api/v1/auth/login/",
        "status_code": 200,
        "response_time": 0.15,
        "timestamp": "2025-01-08T19:00:00Z"
      }
    ],
    "total": 2000
  }
}
```

## 错误处理

### 常见错误码

| 错误码 | 说明 | 解决方法 |
|--------|------|----------|
| 400 | 请求参数错误 | 检查请求参数格式和必填项 |
| 401 | 未授权 | 检查 Token 是否有效 |
| 403 | 权限不足 | 检查用户权限 |
| 404 | 资源不存在 | 检查资源 ID 是否正确 |
| 429 | 请求过于频繁 | 降低请求频率 |
| 500 | 服务器错误 | 联系管理员查看日志 |

### 错误响应示例

```json
{
  "code": 400,
  "message": "参数验证失败",
  "data": {
    "errors": {
      "email": ["邮箱格式不正确"],
      "password": ["密码长度至少8位"]
    }
  },
  "timestamp": "2025-01-08T19:00:00Z",
  "request_id": "req-error-123"
}
```

## 最佳实践

### 1. 使用 Token 认证

```typescript
// 在请求头中添加 Token
const token = localStorage.getItem('token')
axios.defaults.headers.common['Authorization'] = `Bearer ${token}`
```

### 2. 处理响应

```typescript
const response = await apiService.getUserList()

if (response.code === 200) {
  // 成功处理
  const users = response.data.list
} else {
  // 错误处理
  toast.error(response.message)
}
```

### 3. 错误处理

```typescript
try {
  const response = await apiService.createUser(userData)
  if (response.code === 201) {
    toast.success('创建成功')
  }
} catch (error: any) {
  if (error.response?.status === 401) {
    // 未授权，跳转登录
    router.push('/login')
  } else {
    toast.error('请求失败，请稍后重试')
  }
}
```

### 4. 分页处理

```typescript
const fetchUsers = async (page: number = 1) => {
  const response = await apiService.get(`auth/user-management/?page=${page}`)
  
  if (response.data.code === 200) {
    users.value = response.data.data.list
    totalPages.value = response.data.data.total_pages
    hasNext.value = response.data.data.has_next
  }
}
```

## 限流说明

为保护服务器资源，部分接口实施了限流策略：

- **登录接口**: 每分钟最多 5 次
- **AI 生成接口**: 每分钟最多 10 次
- **普通接口**: 每分钟最多 60 次

超过限流将返回 `429 Too Many Requests` 错误。

## 版本说明

当前 API 版本：`v1`

API 路径格式：`/api/v1/{模块}/{功能}/`

## 相关文档

- [backend/docs/api_response_format.md](backend/docs/api_response_format.md) - 详细的响应格式说明
- [开发准则.md](开发准则.md) - 开发规范
- [README.md](README.md) - 项目说明

## 联系方式

如有 API 使用问题，请通过以下方式联系：

- GitHub Issues: [项目 Issues]
- 邮箱: 631008153@qq.com

